fun flash(led) {
    fun on() {
        gpio_put(led, true);
        sleep_ms(500);
        yeild();
    }
    flasher = make_coro(on);
    fun off() {
        gpio_put(led, false);
        sleep_ms(500);
        resume(flasher);
    }
    resume(make_coro(off));
}